apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: api-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: ReplicaSet
    name: api
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 50
